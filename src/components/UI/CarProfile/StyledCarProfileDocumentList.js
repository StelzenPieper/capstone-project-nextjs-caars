import styled from 'styled-components';

const StyledCarProfileDocumentList = styled.div`
	width: 100%;
	margin: 10px 0 10px 0;
	padding: 1vh 0 0 8vw;
	background: var(--transparent);
	display: flex;
	flex-direction: row;
	flex-wrap: wrap;
	gap: 40px;
	align-items: center;
	justify-content: flex-start;
	position: relative;
`;
export default StyledCarProfileDocumentList;
