import styled from 'styled-components';

const StyledCarProfileDocumentHeader = styled.div`
	margin: 30px 0 20px 0;
	padding: 2vh 0 0 8vw;
	background: var(--transparent);
	display: flex;
	flex-direction: row;
	gap: 40px;
	align-items: center;
	justify-content: flex-start;
	position: relative;
`;
export default StyledCarProfileDocumentHeader;
