import styled from 'styled-components';

const StyledImageSliderWrapper = styled.div`
	height: 40vh;
	width: 100%;
	display: flex;
	flex-direction: column;
	overflow: hidden;
	object-fit: cover;
`;

export default StyledImageSliderWrapper;
