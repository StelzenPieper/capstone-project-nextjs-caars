import styled from 'styled-components';

const StlyedImageSlider = styled.div`
	height: 100%;
	width: auto;
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	align-content: center;
	position: relative;
`;
export default StlyedImageSlider;
