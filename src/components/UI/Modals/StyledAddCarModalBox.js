import styled from 'styled-components';

const StyledAddCarModalBox = styled.div`
	width: 85%;
	height: auto;
	margin: 20px;
	border-radius: 8px;
	box-shadow: var(--box-shadow);
	background: var(--primary-background);
	display: flex;
	flex-direction: column;
	justify-content: center;
	align-items: center;
`;

export default StyledAddCarModalBox;
