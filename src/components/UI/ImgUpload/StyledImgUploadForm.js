import styled from 'styled-components';

const StyledImgUploadForm = styled.form`
	margin-top: 15px;
	text-align: center;
	color: var(--primary-color);
	z-index: 101;
`;

export default StyledImgUploadForm;
